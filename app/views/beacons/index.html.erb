<p id="notice"><%= notice %></p>

<h1>Beacons</h1>

<table>
  <thead>
    <tr>
      <th>Uuid</th>
      <th>Major</th>
      <th>Minor</th>
      <th>Alerttitle</th>
      <th>Alertmessage</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @beacons.each do |beacon| %>
      <tr>
        <td><%= beacon.uuid %></td>
        <td><%= beacon.major %></td>
        <td><%= beacon.minor %></td>
        <td><%= beacon.alertTitle %></td>
        <td><%= beacon.alertMessage %></td>
        <td><%= link_to 'Show', beacon %></td>
        <td><%= link_to 'Edit', edit_beacon_path(beacon) %></td>
        <td><%= link_to 'Destroy', beacon, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Beacon', new_beacon_path %>
