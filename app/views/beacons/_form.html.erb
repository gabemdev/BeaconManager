<%= form_for(beacon) do |f| %>
  <% if beacon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beacon.errors.count, "error") %> prohibited this beacon from being saved:</h2>

      <ul>
      <% beacon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uuid %>
    <%= f.text_field :uuid %>
  </div>

  <div class="field">
    <%= f.label :major %>
    <%= f.number_field :major %>
  </div>

  <div class="field">
    <%= f.label :minor %>
    <%= f.number_field :minor %>
  </div>

  <div class="field">
    <%= f.label :alertTitle %>
    <%= f.text_field :alertTitle %>
  </div>

  <div class="field">
    <%= f.label :alertMessage %>
    <%= f.text_field :alertMessage %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
